import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  content: {
    position: 'absolute',
    flex: 1,
    padding: 16,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
